Rezo Lash - New Features Included

1) Email notification on every new booking
Required Vercel environment variables:
- RESEND_API_KEY
- BOOKING_NOTIFY_EMAIL
- EMAIL_FROM

2) Monthly admin calendar page
Path:
- /admin/calendar

3) UI improvements
- Confirmed bookings highlighted in green
- Pending bookings highlighted in amber for attention
- Booked slots on booking page now show as: غير متاح

4) After upload
Run npm install so the new packages are installed.
Then redeploy on Vercel.
